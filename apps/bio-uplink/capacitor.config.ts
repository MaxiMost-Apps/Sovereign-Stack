import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maximost.biouplink',
  appName: 'Maximost Bio-Uplink',
  webDir: 'dist'
};

export default config;
